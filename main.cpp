/*
Notes:

CPU Order of Operations:
	1) Read byte at Program Counter
	2) Use byte (opcode) to index array to find addressing mode and cycles
	3) Read 0, 1, or 2 more bytes for the instruction (arguments)
	4) Execute
	5) Wait, count clock cycles, complete


*/

#include <iostream>

int main() {
	return 0;
}